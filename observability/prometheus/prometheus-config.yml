global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: game-reviewer-api
    static_configs:
      - targets: ["server:3000"]
    metrics_path: /api/metrics